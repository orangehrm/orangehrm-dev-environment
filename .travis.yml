dist: trusty

language: php
php: 5.5

services:
  - docker

cache:
    directories:
        - vendor
        - $HOME/.composer/cache

before_install:
  - docker version
  - composer self-update

install:
  - composer install
  - docker-compose up -d

script:
  - docker ps
  - php vendor/bin/codecept run acceptance
  - docker ps -a
  - docker logs dev_mysql




